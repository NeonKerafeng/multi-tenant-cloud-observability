apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data

spec:
  accessModes:
    - ReadWriteOnce

  # storageClassName intentionally omitted.
  # Base uses the cluster default StorageClass.
  # Environment overlays may replace it.

  resources:
    requests:
      storage: 5Gi
